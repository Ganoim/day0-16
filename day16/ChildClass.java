package day16;

public class ChildClass extends ParentClass { //ParentClass(부모클래스)에 기본생성자가 있어서 상속해서 사용할수있다

/*	public ChildClass(int parentInt, String parentStr) { //ParentClass(부모클래스)에 기본생성자를 만들지 않고 자식클래스에서 superclass를 만들어서 해결할 수 도있다
		super(parentInt, parentStr); // super는 부모클래스를 의미
	} */

	public ChildClass() {
		super();
	}
	
/*	public ChildClass(int parentInt, String parentStr) {
		super(parentInt, parentStr);
		
	} */
	
	
	
	
	
	
	
	

}
